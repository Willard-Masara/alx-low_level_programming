#include "main.h"
/**
 * main-entry ppoint
 * description-positve or negative output
 * return: always 0
 */

void positive_or_negative(int i)
{
	if (i < 0)
		printf(" % d is negative\n", i);
	else
		printf(" % d is positive\n", i);

}

