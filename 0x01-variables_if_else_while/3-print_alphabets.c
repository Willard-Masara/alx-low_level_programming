#include <stdio.h>
/**
 *main- main function on betty
 *
 *return: will be 0
 */
int main (void)
 
{
	for (i = 'a'; i <= 'z'; i++);
 	
		putchar(1);
	for (i = 'A'; i <= 'Z'; i++);
 	
		putchar(i);
	putchar(i);
	return (0);
}
