#include "main.h"
/**
 * _strlen- to return strlen
 * @s: str
 * Return: lstrlen
 */
int _strlen(char *s)
{
	int very_long = 0;
	while (*s != '\0')
	{
	very_long++;
	s++;


	}	


	return (very_long);
}
