#include "main.h"
/**
 * _isalpha- checks for an alphabetical char
 * @c: cha rto b checks
 * Return; 0 or 1
 */
int is_alpha(int c)
{

	return ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'));






}
