#include "main.h"
/**
 * _memcpy-opies memory area/ buffer
 * @dest: storage
 * @src: origin
 * @n: no of bytes to  be stored
 *
 * Return: memory copied
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	int a;
	int b;
 	int c;
	b = c;
	for (a = 0; a < b; a++)
	{

	dest[a] = src[a];
	n--;

	}
	return (dest);






}
