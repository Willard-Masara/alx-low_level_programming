#include "main.h"
#include <stdio.h>
/**
 * main - check the code
 *@n-the number of values
 * Return: Always 0.
 */
void print_numbers(void)
{

	int n;

	for (n = 48; n < 58; n++)
	{
	putchar(n);
	}
	putchar(10);


}
